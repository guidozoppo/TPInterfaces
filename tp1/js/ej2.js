"use strict";

let canvas = document.getElementById("myCanvas");
let ctx = canvas.getContext("2d");
ctx.fillStyle = "#AA2233"; //asigno color
ctx.fillRect(0, 0, 500, 500); // fillRect(x, y, ancho dibujo, alto dibujo)